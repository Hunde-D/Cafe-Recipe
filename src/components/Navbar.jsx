import coverImage from "../assets/bg-cafe.jpg";
const Navbar = ({ className }) => {
  return (
    <div className={className}>
      <img src={coverImage} className="w-full" />
      {/* nav */}
    </div>
  );
};

export default Navbar;
